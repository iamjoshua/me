---
interface Props {
  title: string;
  excerpt: string;
  position?: string;
  id: string;
}

const { title, excerpt, position, id } = Astro.props;
---

<a
  href={`/questions/${id}`}
  class="block rounded-lg hover:scale-101 hover:bg-neutral-50 p-5 -m-5 transition-all"
>
  <h3 class="text-xl font-semibold mb-3 text-gray-900">
    {title}
  </h3>
  <p class="text-gray-600 mb-4 leading-relaxed">
    {excerpt}
  </p>
  {
    position && (
      <div class="text-gray-800 mb-4">
        <div class="italic text-xs uppercase">Position:</div>
        <div class="font-bold">{position}</div>
      </div>
    )
  }
  <div class="flex justify-end">
    <span class="hidden text-sky-700 hover:text-blue-800 text-sm font-medium">
      Read more →
    </span>
  </div>
</a>
